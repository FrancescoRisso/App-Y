import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'politoi.app-y.group4',
  appName: 'App-Y',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
